export const environment = {
  production: true,
  main_url: 'http://localhost:3000',
  log: {
    backend: {
      request: {
        register: true
      },
      response: {
        register: true
      }
    }
  },
};
